services:
  nya-proxy:
    build: .
    image: k3scat/nya-proxy:latest
    restart: unless-stopped
    ports:
      - "127.0.0.1:8080:8080"
    volumes:
      # Create config.yaml first and replace every placeholder credential.
      - ./config.yaml:/app/config.yaml:ro
